load "System";

Fibonacci(x) => a from {
	a = [0, 1];
	i in [3 : x] do {
		a = a ++ [ a[i - 1] + a[i - 2] ];
	};
};

#args == 0 then {
	Console.printLine(sarr(Fibonacci(10)));
} else {
	Console.printLine(sarr(Fibonacci(value(args[1]))));
};

@ Alternatively, the function can be done inline like so:
@ [0 : 10] >> ( (x, a) -> { #a > 1 then a[#a] + a[#a - 1] else x } )
@ This uses the >> operator, sending the array as the input arguments to a lambda one at a time.
@ This feature does not fully work yet - it is still planned

load "System.ruo";

hello() => { System.printLine("Hello"); };
salve() => { System.printLine("Salve"); };
ciao() => { System.printLine("Ciao"); };

thread(
	hello(), 
	salve(), 
	ciao()
);
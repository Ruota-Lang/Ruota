load "System";

threadTest() => {
	func(t) => { 
		i in [1 : 5] do {
			Console.printLine("Thread " .. t .. ":\t" .. i); 
		}; 
	};
	Console.printLine("Unthreaded:");
	func(1), func(2), func(3);
	Console.printLine("Threaded:");
	thread(func(1), func(2), func(3));
};

threadTest();